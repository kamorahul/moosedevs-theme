[project]
name = "moosedevs_theme"
authors = [
    { name = "Moosedevs Limited", email = "hello@moosedevs.com" }
]
description = "Moosedevs ERP branding and white-label customization"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
